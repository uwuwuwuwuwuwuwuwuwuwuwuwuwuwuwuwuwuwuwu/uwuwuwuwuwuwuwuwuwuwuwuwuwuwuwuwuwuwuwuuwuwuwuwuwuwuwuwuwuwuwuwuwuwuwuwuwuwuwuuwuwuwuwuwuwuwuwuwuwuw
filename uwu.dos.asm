;;; Assemble with:
;;; nasm -fbin -o reee.com reee.asm

        org     100h

        mov     ah, 02h
        mov     dl, 'uwu'
        int     21h
        mov     dl, 'uwu'
uwuuwuuwu: int     21h
        jmp     uwuuwuuwuuwu
